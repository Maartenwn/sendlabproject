import { Component } from '@angular/core';

@Component({
    selector: 'mqtt-listen',
    styleUrls: ['mqtt-listen.component.scss'],
    templateUrl: 'mqtt-listen.component.html', 
})
export class MqttListenComponent {
    constructor() {}
}